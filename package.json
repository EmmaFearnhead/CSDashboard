{
  "name": "conservation-dashboard",
  "version": "1.0.0",
  "description": "Conservation Solutions Translocation Dashboard - Railway Deployment",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && python -m uvicorn server:app --host 0.0.0.0 --port $PORT",
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && uvicorn server:app --reload"
  },
  "engines": {
    "node": "18.x",
    "python": "3.11.x"
  }
}